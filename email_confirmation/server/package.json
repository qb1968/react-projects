{
  "name": "server",
  "version": "1.0.0",
  "description": "server for email_confirmation",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production forever server.js",
    "deploy": "now && now alias",
    "dev": "nodemon server"
  },
  "author": "Jonathan Allison",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "forever": "^3.0.4",
    "mongoose": "^5.11.2",
    "nodemailer": "^6.4.16"
  },
  "devDependencies": {
    "nodemon": "^2.0.6",
    "now": "^21.0.1"
  }
}
